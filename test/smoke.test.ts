import { describe, it, expect } from "vitest";

/**
 * Phase 0 placeholder — proves the vitest gate runs. Removed in Phase 1 when
 * the first real test lands.
 */
describe("smoke", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
